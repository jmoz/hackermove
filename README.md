# Hackermove

## Usage

```
$ python -m hackermove -h
usage: __main__.py [-h] [--location LOCATION] [--beds BEDS] [--minbeds MINBEDS] [--maxbeds MAXBEDS] [--minprice MINPRICE] [--maxprice MAXPRICE]

options:
  -h, --help           show this help message and exit
  --location LOCATION  Location search string
  --beds BEDS          Beds
  --minbeds MINBEDS    Minimum beds
  --maxbeds MAXBEDS    Maximum beds
  --minprice MINPRICE  Minimum price
  --maxprice MAXPRICE  Maximum pric
```
